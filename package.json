{
  "name": "planimulation",
  "version": "0.1.0",
  "private": true,
  "description": "An exploratory desktop simulation of procedural worlds",
  "type": "module",
  "main": "dist/electron/main.cjs",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "build": "npm run typecheck && node --import tsx scripts/build.ts",
    "start": "npm run build && electron .",
    "desktop": "electron .",
    "headless": "node --import tsx src/headless.ts",
    "benchmark": "node --import tsx scripts/benchmark.ts",
    "test:desktop": "npm run build && node --import tsx scripts/test-desktop.ts",
    "package": "npm run build && node --import tsx scripts/package.ts"
  },
  "devDependencies": {
    "@electron/packager": "20.3.0",
    "@playwright/test": "1.63.0",
    "@types/node": "26.6.2",
    "electron": "44.4.3",
    "esbuild": "0.28.2",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
